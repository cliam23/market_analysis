# Optional: official U.S. CPI for the equity chart “cash baseline” (same purchasing power as start).
# Free API key: https://fred.stlouisfed.org/docs/api/api_key.html
# Without this, the server uses a ~3%/yr compound rule-of-thumb.

